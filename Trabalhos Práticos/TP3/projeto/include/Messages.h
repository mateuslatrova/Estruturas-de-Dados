#ifndef MESSAGES_H
#define MESSAGES_H

#include <string>

// Typedef commands to make an easier code comprehension about
// the sent and received messages by the control center.

typedef std::string Alphabet;
typedef std::string EncodedMessage;
typedef std::string DecodedMessage;
typedef char CommandType; // A, C or D.

#endif